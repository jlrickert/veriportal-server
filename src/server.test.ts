describe("Graphql server", () => {
  it("Should authenticate user when available", () => {
    expect(5).toEqual(3);
  });

  it("Should not find user when not available", () => {
    expect(5).toEqual(3);
  });
});
